module tb;
 // int my_array [int];
  //integer my_array [integer];
 // logic my_array [];
 // reg my_array [];
  parameter SIZE=3;
  reg my_array [SIZE-1:0];
  reg index;
  
  
  initial begin
   //if(my_array.num() > 0)
   // if (my_array.size() > 0)
      //if( index < my_array.size()) 
    if ( index >=0 && index < SIZE)  
      $display("my_array = %p" , my_array);
    
  end
  
endmodule  
      
      
      
